CONTIKI_PROJECT = hello-world
all: $(CONTIKI_PROJECT)

DAO_INDUCTION_CLIENT ?= 0
DAO_ATTACK_PERIOD_SECONDS ?= 15
FILTER_BR_DIO ?= 0
CFLAGS += -DDAO_INDUCTION_CLIENT=$(DAO_INDUCTION_CLIENT)
CFLAGS += -DDAO_ATTACK_PERIOD_SECONDS=$(DAO_ATTACK_PERIOD_SECONDS)
CFLAGS += -DFILTER_BR_DIO=$(FILTER_BR_DIO)

CONTIKI = ../..

MODULES += os/services/shell

include $(CONTIKI)/Makefile.include

